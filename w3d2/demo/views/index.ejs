<h3>Why no jokes :( <a href="/jokes/new">add joke?</a></h3>
<section>
    Jokes so empty, its funny
    <% for(const jokeID in locals.jokes) {%>
        <% const joke = locals.jokes[jokeID] %>
        <article
            style="box-shadow: 0px 0px 5px 0px gray; border-radius: 4px; padding: 20px 40px; margin: 40px auto;
            max-width: 600px;"
        >
            <p><%= joke.text %></p>
            <em><%= users[joke.userID] %></em>

            <footer>
                <a href="/jokes/<%= jokeID %>">View</a>
                <a href="#">Edit</a>
                <form action="/jokes/<%= jokeID %>/delete" method="POST">
                    <button>Delete</button>
                </form>
            </footer>
        </article>
    <% } %>
</section>

