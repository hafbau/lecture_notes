// Conditional
if (true) {
  // do thing
} else {
  // do other
}

// Loops
for (let i = 0; i < 100; i++) {
  // do stuff 100 times
}

// Objects
// - storage
const person = {};
person.name = 'Hafiz Suara';
person['isCool'] = 'definitely!';

// - access
console.log(person.isCool);
console.log(person['name']);