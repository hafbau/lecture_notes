/* Question 0

Implement a function "tee", which, when given a value, console.logs the value and then returns the value.

For example:

  tee(5);

Logs:

  5

Returns:

  5

*/


function tee(value) {

}

// Your personal test code:

console.log(tee(5));
console.log(tee(5) === 5);

// Don't change below:

module.exports = tee;
