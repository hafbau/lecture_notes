"use strict";

/* Question 05

Capital case is a specific method of capitalizing the characters of every word in a sentence in order to make it usable as a title or headline. Construct the body of the function called titleCase() that takes a string as the single parameter.

Examples:

- titleCase("this is an example") returns "This Is An Example"
- titleCase("test") returns "Test"
- titleCase("i r cool") returns "I R Cool"
- titleCase("WHAT HAPPENS HERE") returns "What Happens Here"
- titleCase("") returns ""
- titleCase("A") returns "A"

*/

const titleCase = function(text) {

  /* IMPLEMENT ME */

};


// Don't change below:

module.exports = { titleCase };
